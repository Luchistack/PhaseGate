const prompt = require("prompt-sync")();


let length = parseInt(prompt("Enter length of a triangle: "));

let width = parseInt(prompt("Enter width of a triangle: "));

let area = length * width;


console.log("Are of triangle is: "+ area);



